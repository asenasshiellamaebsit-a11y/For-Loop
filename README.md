# For-Loop
Write a program using for loop

#include <iostream>
using namespace std;

int main() {
    cout << "Using for Loop: " << endl;
    for (int l = 1 ; l <= 5; l++) {
        cout << l << "\n";
    }
    return 0;
}
